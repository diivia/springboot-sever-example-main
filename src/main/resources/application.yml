server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:textkey;MODE=LEGACY
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  liquibase:
    enabled: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
